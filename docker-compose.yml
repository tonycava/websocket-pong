services:
  pong:
    build: .
    image: tonycava/pong:latest
    ports:
      - "8080:80"
    restart: always
  back:
    build:
      dockerfile: Dockerfile.back
      context: .
    image: tonycava/pong-back:latest
    ports:
      - "3000:3000"
    restart: always